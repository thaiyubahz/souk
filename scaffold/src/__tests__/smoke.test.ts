import { describe, it, expect } from 'vitest'

// Placeholder smoke test so CI has a green run from day one of Phase 0.
// Real tests land in Phase 3.

describe('smoke', () => {
  it('test runner is wired up', () => {
    expect(true).toBe(true)
  })
})
